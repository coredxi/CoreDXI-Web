# Claude Code 작업 프롬프트 — AX 체크 경험 개선 (2026-08-30)

> 사용법: 아래 프롬프트 전문을 복사해 `C:\MyProjects\CoreDXI-Web`에서 실행한 Claude Code 세션에 붙여넣으세요.

---

```
/ax-check(AX 체크 깔때기)의 경험 개선 작업을 진행해줘. 근거 문서는
docs/superpowers/plans/2026-08-30-ax-check-experience-upgrade-action-plan.md 이고,
작업 전에 반드시 이 문서 전체와 기존 설계
docs/superpowers/specs/2026-08-22-sales-funnel-ax-check-design.md 를 읽고 시작해.

## 목적

영업채널 고도화의 목적은 우리 회사의 AX 전환 컨설팅을 소개하고, 고객사가 AI·AX에
거부감을 덜 느끼고 쉽게 접근하게 하는 것이다. 이 목적에 맞춰 아래 3가지를 구현한다.

## 착수 전 (프로젝트 지침 5-1 — 문서 우선)

1. 액션 플랜을 근거로 docs/superpowers/specs/2026-08-30-ax-check-experience-upgrade-design.md
   설계 문서를 작성한다 (기존 CSP 설계 문서 패턴 준수).
2. docs/PRD.md 와 docs/TODO.md(1-B 절)에 이번 변경을 먼저 반영한다.
   특히 원 설계의 "상세본 자동 메일 발송"을 철회하고 "초안 생성 + 영업이사 수동 발송"으로
   바꾼다는 결정을 명시할 것.
3. 브랜치 feat/ax-check-experience-upgrade 를 main에서 새로 딴다.
   (주의: feat/funnel-dashboard-stage2 같은 기존 브랜치 위에 이어붙이지 말 것)

## 작업 C — 이메일 초안 워크플로우 (가장 먼저, 첫 링크 발송 전 필수)

- src/actions/ax-check.ts 에서 고객 대상 자동 메일 발송(sendResendEmail)을 제거한다.
  영업이사 알림 메일(SALES_NOTIFY_EMAIL)은 유지하되, 본문 하단에 고객용 이메일 초안
  전문을 동봉한다.
- 신규 모듈 src/lib/ax-check/email-draft.ts: buildCustomerEmailDraft(answers, summary,
  contact) 순수 함수. 초안은 DB에 저장하지 않고 조회 시점에 생성한다(스키마 변경·
  마이그레이션 없음). 구성: ① 회사명·성함 인사 ② 우선 과제 상세(작업 B 확장 카드)
  ③ CoreDXI 진행 방식 1문단(진단→설계→구축→교육) ④ 다음 단계 제안 + 영업이사 서명
  블록(catalog.ts 상수) ⑤ [[통화에서 말씀 주신 ___]] 형태의 수동 편집 슬롯 1곳.
- /admin/leads 리드 상세에 "이메일 초안" 패널: 전문 미리보기 + "초안 복사" 버튼(주 경로,
  클라이언트 컴포넌트·CSP nonce 정책 준수) + mailto: 링크(보조 — 본문 잘림 가능성 주석).
- 결과 화면(/ax-check/result/[token])과 제출 완료 문구의 "메일로 보내드렸습니다"를
  "상세 진단서는 담당 이사가 직접 검토해 1영업일 내 메일로 보내드립니다"로 교체한다.
- /privacy 의 수집·이용 목적 문구가 "담당자 검토 후 발송"과 모순되지 않는지 점검하고
  필요하면 고친다.

## 작업 A — 인트로 화면

- src/app/ax-check/AxCheckIntro.tsx 신규: 별도 게이트 화면이 아니라 같은 페이지 상단
  섹션. 하단 "3분 진단 시작하기" 버튼은 폼 앵커로 스크롤(완료율 보호를 위해 클릭 수를
  늘리지 않는다). ?ref= 파라미터 동작에 영향 없어야 함.
- 내용: ① 한 줄 정체성("중소기업의 AI 도입·AX 전환을 설계부터 교육까지 함께하는 컨설팅")
  ② AX 전환 컨설팅 4단계 요약(어려운 용어 금지, "반복 업무를 줄이는 일"의 언어로)
  ③ 거부감 완화 블록: "AI를 몰라도 됩니다 — 모든 질문이 선택지", "3분 8개 질문",
  "제출해도 영업 전화가 자동으로 가지 않습니다 — 결과는 화면에서 바로, 상세 진단서는
  담당 이사가 직접 검토해 메일로" ④ 결과 미리보기 카드 1장 ⑤ CTA.
- 카피는 전부 catalog.ts의 INTRO_COPY 상수로 두어 홍보팀 수정 경로를 유지하고,
  CONTENT_GUIDE.md 17번에 수정 방법을 추가한다.
- 디자인: shadcn/ui 우선, 로열 블루 #1E4E8C, 코너 반경 0.75rem 이상, 여백 중심,
  WCAG AA 대비(기존 다크모드 검증 패턴 재사용), 모바일 360px에서 1.5화면 이내.

## 작업 B — 결과 피드백 구체화 (LLM 금지 — 규칙 기반 유지)

2026-08-22 결정(응답 20건 누적 전 LLM 도입 금지)은 그대로다. catalog.ts 콘텐츠 확장으로만
구체성을 올린다:
1. 답변 에코: 각 과제 카드 상단에 "'제안서·견적서 작성'을 가장 시간이 많이 드는 업무로
   꼽으셨습니다"처럼 선택 라벨을 그대로 인용하는 근거 문장.
2. Q1(업종)×Q3(업무) 업종별 예시 맵(industryExamples): AV 업체엔 "장비 구성표·시공 내역
   기반 견적 초안", 네트워크 업체엔 "BOM·회선 구성 기반 제안 초안" 같은 업종 언어 1~2문장.
3. firstStep을 미니 로드맵 3단계로 확장(첫 1주 / 첫 1개월 / 3개월). Q2(규모)·Q4(성숙도)
   분기 — 10명 미만이면 "전담자 없는 SaaS부터", "전혀 없음"이면 "기초 교육부터".
4. 기대 효과에 보수적 범위 수치 + "일반적 도입 사례 기준" 명시. 확정 수치·보장 표현 금지.
- AxCheckSummary 타입 확장(echo/industryExample/roadmap 필드), 결과 화면과 이메일 초안이
  같은 데이터를 소비하게 한다. 모든 신규 문구는 catalog.ts 데이터로만(로직 하드코딩 금지).

## 테스트·품질 (지침 5-2·5-4)

- Vitest: email-draft.ts 신규 테스트, summarize.test.ts에 업종·규모·성숙도 분기와 에코
  문장 테스트 추가. 서로 다른 Q1·Q2·Q4 조합 3세트가 눈에 띄게 다른 결과를 내는지 확인.
- Playwright E2E: 결과 화면 문구 변경·고객 메일 발송 제거로 기존 골든패스가 깨진다 —
  같은 브랜치에서 반드시 수정(PR #6에서 고친 이메일 필드 정확 일치 로케이터 주의).
- pnpm lint && npx tsc --noEmit && pnpm test 전부 통과 후 커밋.
- 이번 작업에 DB 마이그레이션은 없다. prisma migrate dev는 어떤 경우에도 실행 금지.
- 커밋은 Conventional Commits로 A/B/C 작업 단위 분리. 마지막에 docs 커밋으로
  TODO.md 1-B 진행 현황을 갱신한다.

## 완료 보고

끝나면 다음을 요약해줘: 변경 파일 목록, 남은 사람 작업(영업이사 서명 블록 실제 정보 입력,
인트로·초안 카피 검수, 실기기 테스트), 그리고 첫 링크 발송 전 체크리스트.
```
